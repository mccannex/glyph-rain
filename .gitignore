# Build output (CMake, vendored SDL2 via FetchContent, Visual Studio project files)
build/
build-*/

# Legacy pre-rewrite source, kept locally for reference only — not our code
legacy/

# Planning docs — local dev notes only, not part of the shipped project
planning/

# Claude Code local config
.claude/

# Intermediate build-tool output (tools/generate_glyph_atlas.py) -- not read
# by any code at runtime, the real asset is the committed core/glyph_atlas_data.h
assets/fonts/*.bmp

# OS/editor cruft
.DS_Store
Thumbs.db
.vs/
*.user
